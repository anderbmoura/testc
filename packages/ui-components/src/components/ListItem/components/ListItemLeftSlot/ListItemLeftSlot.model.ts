import { ReactNode } from 'react';

/**
 * Propriedades para o ListItemLeftSlot
 */
export interface ListItemLeftSlotProps {
  /**
   * Conteúdo do slot - aceita qualquer elemento React
   */
  children: ReactNode;
}
