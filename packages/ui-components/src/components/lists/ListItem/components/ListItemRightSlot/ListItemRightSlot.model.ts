import { ReactNode } from 'react';

/**
 * Propriedades para o ListItemRightSlot
 */
export interface ListItemRightSlotProps {
  /**
   * Conteúdo do slot - aceita qualquer elemento React
   */
  children: ReactNode;
}
