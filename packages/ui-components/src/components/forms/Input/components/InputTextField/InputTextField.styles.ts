import { styled } from 'tamagui';
import { View } from 'react-native';

export const StyledTextInputContainer = styled(View, {
  name: 'StyledTextInputContainer',
  flex: 1,
  width: '100%',
  alignItems: 'flex-start',
  justifyContent: 'flex-start',
});
