import { styled, XStack } from 'tamagui';

export const StyledDigitRow = styled(XStack, {
  name: 'StyledDigitRow',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '$nano',
  width: '100%',
});
