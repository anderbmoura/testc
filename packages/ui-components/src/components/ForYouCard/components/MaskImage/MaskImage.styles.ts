import { View, styled } from 'tamagui';

export const MaskContainer = styled(View, {
  name: 'MaskContainer',
  position: 'absolute',
  top: -46,
  left: -12,
  width: 190,
  height: 194,
  zIndex: '$100',
});
