import { View, styled } from 'tamagui';

export const BackgroundContainer = styled(View, {
  name: 'BackgroundContainer',
  position: 'absolute',
  top: -40,
  left: -10,
  width: 209,
  height: 181,
});
