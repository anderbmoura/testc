import { View } from 'react-native';
import { styled } from 'tamagui';

export const RemoveButtonContainer = styled(View, {
  name: 'CardWidgetRemoveButtonContainer',
  position: 'absolute',
  left: -8,
  top: -8,
});
