import type { CardAlertVariant } from '../../CardAlert.model';

export interface CardAlertIconProps {
  /**
   * Visual variant that determines the icon and color.
   */
  variant: CardAlertVariant;
}
