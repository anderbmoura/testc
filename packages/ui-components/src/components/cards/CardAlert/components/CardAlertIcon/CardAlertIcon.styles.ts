import { View, styled } from 'tamagui';

export const IconContainer = styled(View, {
  name: 'CardAlertIconContainer',
  width: 24,
  height: 24,
  flexShrink: 0,
  justifyContent: 'center',
  alignItems: 'center',
});
