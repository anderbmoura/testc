import { View, styled } from 'tamagui';

export const CardNotificationStackContainer = styled(View, {
  name: 'CardNotificationStackContainer',
  position: 'relative',
  width: '100%',
});

export const CardNotificationStackItem = styled(View, {
  name: 'CardNotificationStackItem',
  position: 'absolute',
  top: 0,
  left: 0,
  right: 0,
  width: '100%',
});
