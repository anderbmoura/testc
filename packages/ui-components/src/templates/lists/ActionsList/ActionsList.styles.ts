import { styled, YStack } from 'tamagui';

/**
 * Container principal do template ActionsList
 */
export const StyledActionsListContainer = styled(YStack, {
  name: 'ActionsListContainer',
  width: '100%',
  gap: '$nano',
});
