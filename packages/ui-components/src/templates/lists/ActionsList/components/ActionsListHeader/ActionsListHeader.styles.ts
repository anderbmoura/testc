import { styled, View } from 'tamagui';

/**
 * Container para o cabeçalho do ActionsList
 */
export const ActionsListHeaderContainer = styled(View, {
  name: 'ActionsListHeaderContainer',
  width: '100%',
  overflow: 'hidden',
});
