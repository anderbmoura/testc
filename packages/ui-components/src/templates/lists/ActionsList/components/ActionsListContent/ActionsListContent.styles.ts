import { styled, YStack } from 'tamagui';

/**
 * Container para o conteúdo do ActionsList
 */
export const ActionsListContentContainer = styled(YStack, {
  name: 'ActionsListContentContainer',
  width: '100%',
  paddingHorizontal: '$tiny',
});
