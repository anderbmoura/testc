import { styled, YStack } from 'tamagui';

/**
 * Container principal do template OptionsList
 */
export const StyledOptionsListContainer = styled(YStack, {
  name: 'OptionsListContainer',
  width: '100%',
  gap: '$nano',
});
