import { styled, XStack } from 'tamagui';

export const ButtonRowContainer = styled(XStack, {
  name: 'ButtonRowContainer',
  gap: '$nano',
  width: '100%',
  alignItems: 'stretch',
  justifyContent: 'space-between',
});
