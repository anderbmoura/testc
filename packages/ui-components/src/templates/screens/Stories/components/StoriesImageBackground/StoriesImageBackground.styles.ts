import { styled, View } from 'tamagui';

export const StoriesImageContainer = styled(View, {
  name: 'StoriesImageContainer',
  position: 'absolute',
  top: 0,
  left: 0,
  right: 0,
  bottom: 0,
  zIndex: 1,
});
